The DataCleaing directory should be placed into the same root folder as BMMS,
RMMS, and WBSIM for the program to run properly. The final structure should look
like this:

/
    BMM/
    DataCleaning/
    RMMS/
    WBSIM/
